// #uchsoftware
#comment line

a and b # Her ikisi de dogru veya yanlıs
a or b
not a #a dogru ise basına not ekleyince yanlıs, yanlıs ise basına not ekleyince dogru olması.

#Username and password check control

defuser = "umutcan3398"
defpassword = "3398"

user = input("username:")
password = input ("your password:")
if (defuser == user):
if (password != defpassword):
        print ("Welcome to HES")
elif ((defuser != user) and (password == defpassword)):
        print ("You entered wrong username")
elif (defuser == user) and (password != defpassword):
        print("You entered wrong user

elif ((defuser != user) and (password == defpassword)):
        print ("You entered wrong username")
elif (defuser == user) and (password != defpassword):
        print("You entered wrong user
